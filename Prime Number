#creat list 100-200 
numbers=range(100, 200,1)
#for loop for fine prime number
for num in numbers:
   if num > 1:
       for i in range(2, num):
           if (num % i) == 0:
               break
       else:
           print(num)
